# 学び・方法論（NOTES）

> 要件でも進捗でもない、**進め方・設計判断の理由・ハマりどころ・罠**を残す（メタな学び）。
> 肥大化したら checkpoint 方式（docs/checkpoints/）に逃がす。

最終更新: 2026-09-03

## 立ち上げ時点で持ち込んだ前提（voice-input からの引き継ぎ）
- **Windows ネイティブでしか成立しない**。マイク・ループバック・グローバルホットキーの3つが
  いずれも WSL2 では取れない。編集は WSL、動作確認は Windows。
- **`ANTHROPIC_API_KEY` を環境変数に置かない**。WSL 側の Claude Code が拾って従量課金で動く事故になる。
- ローカル STT は whisper large-v3-turbo が日本語で実績あり。日本語特化モデルは
  英語術語をカタカナ化するため、技術会議の文字起こしでは不利になりうる。
- ただし voice-input は**数秒の音声**の話。**このPJは数十分**なので、同じ結論が使える保証はない。
  持ち込んだ前提として扱い、実測で確かめる。

## （セクションは必要になったら足す）

## ループバック録音（相手の声）は、既存資産でほぼ解決している
調べた結論: **2系統を別トラックで録ること自体は難所ではない**。OBS がやっているのと同じ
WASAPI ループバック（`AUDCLNT_STREAMFLAGS_LOOPBACK`）で、Windows が公式に用意している仕組み。
仮想オーディオデバイスもドライバも要らない。

さらに **voice-input が既に使っている cpal 0.18.2 が対応済み**だった（手元の
`~/.cargo/registry/.../cpal-0.18.2` を読んで確認）:
- `src/host/wasapi/mod.rs:24` — 「出力デバイスを入力デバイスとして使うと透過的にループバックになる」
- `src/host/wasapi/device.rs:855` — `data_flow() == eRender` なら実際にループバックフラグを立てている

つまり「出力デバイスを取って `build_input_stream` を呼ぶ」だけ。マイク側は通常の入力ストリーム。
**録音まわりは voice-input の資産がほぼそのまま効く**、という前提で技術選定してよい。

### ただし本当のリスクは「録った後」に5つある
1. **どの出力エンドポイントを掴むか** — Teams は通常の再生デバイスとは別に「通信デバイス」を
   持てる。既定の出力を機械的に選ぶと無音の WAV ができる。会議中のヘッドセット抜き差しでも変わる。
2. **無音区間にデータが来ない可能性** — ループバックは再生中のアプリが無いとパケットを返さない
   挙動が知られている。サンプル数を数えて時刻にすると、相手が黙った分だけタイムラインが詰まり、
   統合時に自分トラックとずれる。**時刻ベースで無音を埋める設計が要る**。実機で挙動を確認する項目。
3. **マイクへの回り込み** — スピーカーで聞くと相手の声がマイクにも入り、両トラックに同じ発言が
   乗って「自分が言った」ことになる。ヘッドセット運用を前提にするのが一番安い。
4. **クロックドリフト** — マイクと出力は別クロック。60分で数百ms ずれ得る。文単位のラベル付けなら
   許容範囲だが、サンプル単位で突き合わせる設計にすると壊れる。
5. **フォーマットの不一致** — マイクがモノラル / 出力がステレオ、レート違いもある。統合時に揃える。

### 上位互換（v1 では採らない）
Windows 10 2004 以降は**プロセス単位のループバック**が使え、Teams / Slack のプロセスだけ録れる
（通知音や BGM が混ざらない）。cpal にこの API は無く `windows` クレートで直接叩くことになる。
v1 は出力全体で足りる。必要になった時の逃げ道として覚えておく。

> **OBS でできる範囲は、このPJで一番簡単な部分**。本体は録った後
> （文字起こし・時系列統合・要約）にある、という認識で進める。

## 音の経路（ヘッドセット / ネックスピーカー）は録音品質の前提条件
**開放型のスピーカーで会議を聞くと、このPJの本体価値（2系統の分離）が壊れる。**
相手の声がマイクに回り込み、自分トラックにも乗る。文字起こしすると**相手の発言が
「自分:」として二重に出る**。ループバック側は再生ストリームの digital tap なので無傷。
**壊れるのはマイクトラックだけ**、という非対称を押さえておく。

- **ネックスピーカーは選択肢の中で最悪**に近い。肩に載る＝口元から 15〜25cm、内蔵マイクや
  ノートPCのマイクから見ても自分の口とほぼ同じ距離にある。相手の声が自分の声と同程度の
  大きさで入るので、レベル差での分離が効かない。「個人向けで音が漏れにくい」は
  **人に聞こえるか**の話であって、**マイクに入るか**とは別問題。
- **「会議相手にはエコーが聞こえていないから大丈夫」は成立しない。** Teams / Slack のエコー
  キャンセルは**そのアプリが送出する音声**に効く。こちらは WASAPI で生のマイクを別に掴むので、
  その恩恵はゼロ。会議は快適なのに録音だけ壊れる、という気づきにくい失敗になる。
  （デバイス側のオーディオ拡張が効く場合はあるが、当てにしない）
- **Bluetooth で「マイクも使う」と両トラックが劣化する。** 従来の Bluetooth は双方向になると
  HFP/mSBC に落ち、16kHz 程度の狭帯域になる。ループバックはその狭帯域ミックスを掴むので、
  **相手トラックの文字起こし精度まで下がる**。LE Audio 対応機なら回避できることがある。
- → **有線 / USB のヘッドセットを運用前提にする**。安いうえに、上の3つを一度に消す。
  ネックスピーカーを使いたい会議は「要約の質が落ちる会議」として割り切る。

### 回り込みを許した場合の逃げ道（採らないが覚えておく）
ループバックは相手の声の**参照信号そのもの**なので、原理的にはエコー除去できる。ただし適応
フィルタの実装は重い。安く済ませるなら**統合時のテキスト重複除去**（同時刻に重なり、かつ
文面がほぼ同じ自分トラック側の発話を捨てる）。相槌を打つと壊れるが、実装コストは桁違いに低い。

## cpal でループバックを開くときの罠
出力デバイスに対して `default_input_config()` を呼ぶと
**「Device does not support input」で失敗する**（cpal はデータフローで弾く。`device.rs:807`）。
設定は `default_output_config()` から取り、それで `build_input_stream()` を建てる。
「ループバック対応」と「入力デバイス扱い」は別、という非対称がここに出る。

## 【実測・開発デスクトップ】ループバックは無音でもパケットを返した
`proto/dual-capture` を12秒走らせ、**最初の5秒は何も再生せず**、5秒以降に音を鳴らして測った。

```
[相手] VG27AQL1A (NVIDIA High Definition Audio) 48000 Hz / 2 ch / F32
  実経過 12.00 秒 / 録れた長さ 12.00 秒 (576000 フレーム) / ズレ +0.00 秒
  最初のデータ 開始から 0.01 秒後 / ピーク -5.7 dBFS
```

**無音の5秒ぶんもゼロ埋めで返ってきており、サンプル数＝経過時間になっている。**
この時点では「リスク2は起きない、統合を時刻ベースにする必要は無い」と読んだが、
**この読みは後に覆った**（次節）。確認したのが開発デスクトップの NVIDIA HDMI 出力エンドポイント
1つだけだったため、「同じ計測を業務ノートで再実行するまでは決着にしない」と留保を付けていた。
**この留保が効いた。1機種の実測を一般化しかけていた。**

- 副産物1: **WSL から `.exe` を起動しても実デバイスに届く**（Windows プロセスとして走るため）。
  ループバック側の計測は WSL 側から回せる。マイクは実機依存なので業務ノートで確認する。
- 副産物2: `CARGO_TARGET_DIR` を WSL 側で `export` しても **cargo.exe には渡らない**
  （WSLENV を通していないため）。生成物は WSL 側の `target/` に落ちる。
- 副産物3: 12秒で `buffer underrun or overrun` が1回出た。サンプル数は合っていたので実害は
  無かったが、**長時間録音で累積しないかは要観察**。

## 【実測・業務ノート】ループバックは無音でパケットを返さない（機種で挙動が割れる）
業務ノート（ThinkPad L13 Gen 4 / `21LCS4M800` / Intel iGPU）で同じ計測をやり直したところ、
**前節と正反対の結果**になった。既定の出力エンドポイントは`Realtek HD Audio 2nd output`。

| 実行 | 実経過 | 録れた長さ | ズレ | 最初のデータ |
|---|---|---|---|---|
| 12秒（前半6秒無音、後半テスト音） | 12.01秒 | 5.32秒 | **+6.69秒** | 5.75秒後 |
| 30秒（喋り＋再生） | 30.00秒 | 5.59秒 | **+24.41秒** | 0.05秒後 |
| 15秒（喋りのみ、再生ほぼ無し） | 15.00秒 | 0.68秒 | **+14.32秒** | 14.40秒後 |

**3回とも「実際に何かが再生されていた時間ぶんしかパケットが来ない」。**
1回目は最初のデータが5.75秒後で、テスト音を鳴らし始めた6.0秒とほぼ一致した。

つまりリスク2は外れていなかった。**Realtekでは再生中のアプリが無いとパケットが来ない。**
NVIDIA HDMIでゼロ埋めされたのはそのドライバ固有の挙動で、一般化できない。

- **統合はサンプル数を時刻に換算してはいけない。**相手が黙った分だけタイムラインが詰まり、
  自分トラックと合わなくなる。コールバック到着時の実時刻を持ち、無音を時刻で埋める。
- **ドライバ差は設計を左右する。**「実測した」は「その機種で実測した」でしかない。
  録音まわりの前提は、**実際に使う機械で測るまで確定と呼ばない**。

## 【実測】有線イヤホンなら回り込みは起きない（リスク3は条件付きで解消）
30秒を3段に割り、**相手の音だけが鳴っていて自分が黙っている区間**を作って測った。
機材は業務ノートの**内蔵マイク配列＋有線イヤホン**（専用ヘッドセットではない）。

| 段 | 状態 | マイクのRMS | ピーク |
|---|---|---|---|
| 段A（0-10秒） | 喋る / 再生なし | **-34.3 dBFS** | -20.5 dBFS |
| 段B（10-20秒） | **黙る / 再生あり** | **-76.8 dBFS** | -50.0 dBFS |
| 段C（20-30秒） | 喋る / 再生あり | -32.0 dBFS | -13.7 dBFS |

**段Aと段Bの差は42.5 dB**（振幅比で約1/130）。段Bを0.25秒刻みで見るとほぼ全域が-88 dBFS＝
暗騒音そのもので、単発のノイズが1回あるだけ。**相手の音が20秒間鳴り続けている間、
マイクは無音を返した。**

- **ADR-003（ヘッドセット運用を前提にする）は、内蔵マイク＋有線イヤホンでも成立する。**
  専用ヘッドセットが無くても、耳を塞ぐ有線イヤホンであれば分離は保たれる。
- 段の境界に鳴らしたビープ（`[Console]::Beep`）は**相手トラックには0.21秒後として記録されたのに、
  マイクトラックの0〜1.25秒は-88 dBFSのまま**だった。出力へは届き、空気中へは漏れていない。
- **測り方の型**: 回り込みは「相手の音だけが鳴り、自分が黙っている区間」でしか測れない。
  喋りながら鳴らす区間だけでは、マイクのエネルギーがどちらのものか分離できない。
  合図はビープ音で出すと、話者が画面を見ずに済むうえ、**両トラックに境界マーカーが残る**。

## 【実測】既定の出力エンドポイントは耳に届いていた
`Realtek HD Audio 2nd output`（96000 Hz、ヘッドホンジャック）は、実際にイヤホンから聞こえた。
デジタルタップであるループバックは、物理的に何も繋がっていなくても録れてしまうため、
**「WAVに音が入っている」は「聞こえている」の証拠にならない**。今回は合図のビープに従って
操作できたことで、聞こえていることが確かめられた。

## 【実測】ループバックの長さは「再生していた時間」と一致する
上の回り込みテストで、相手トラックは**20.41秒**録れた。再生は10秒目から30秒目までの**20秒間**。
差の0.4秒は境界に鳴らしたビープぶんで説明がつく。

「再生中のみパケットが来る」という結論が、**再生時間と録れた長さの一致**という形で裏付けられた。
統合を時刻ベースにする根拠はこれで固い。

## 出力エンドポイントは抜き差しで消え、既定が勝手に移る（リスク1が実機で出た）
イヤホンを挿していた間の既定は`Realtek HD Audio 2nd output`（96000 Hz）。
イヤホンを抜いたら**このエンドポイントは一覧ごと消え**、既定が`AVT BC110`
（ディスプレイ/キャプチャ系の音声デバイス、48000 Hz）へ移った。

`2nd output`はヘッドホンジャックのエンドポイントで、ジャックの抜き差しで出入りする。
**移った先へ出た音は耳に届かない。**会議中にヘッドセットを抜き差しすると、
既定を機械的に選ぶ実装は無音のWAVを作る。

→ **v1でも「どのエンドポイントを使うか」を設定で固定できる必要がある。**
   `proto/dual-capture`の`--loopback=<名前の一部>`は正しい方向。既定任せにしない。
→ さらに、**録音開始時にエンドポイント名をログへ残す**。後から「無音だったのはなぜか」を
   追えるようにしておく。

## マイクが極小レベルでしか録れない時、まずWindowsの音量設定を疑う
業務ノートの初回計測でマイクのピークが**-48.7 dBFS**（16bitで振幅120 LSB）しか出ず、
喋っているのに環境ノイズと同レベルだった。ミュートでもプライバシーブロックでもなく
（`ConsentStore\microphone`は`Allow`）、**Windowsの録音音量が27%だった**のが原因。

100%に上げて再計測するとピーク**-19.5 dBFS**（RMS -38.6 dBFS）。**29 dB改善**した。

- このエンドポイントの音量スカラーは**dBに線形ではない**。27%が-11.5 dB、100%が+20 dBで、
  差は31.5 dB。実測の29 dBとほぼ一致する。**「27%だから約1/4」ではない。**
- 音量が極小のままだと録音ストリーム自体が死ぬことがあった（30秒の実行で24.88秒以降が
  完全ゼロ、`buffer underrun or overrun`が同時に発生）。100%では再発していない。因果は未確定。
- 読み書きはCore Audioの`IAudioEndpointVolume`をPowerShellから叩けば取れる
  （`GetMasterVolumeLevelScalar` / `SetMasterVolumeLevelScalar` / `GetMute`）。
  `AudioDeviceCmdlets`のような追加モジュールは要らない。

## 業務ノートの内蔵マイクは4chだが中身はモノラルの複製
`マイク配列（インテル スマート・サウンド・テクノロジー）`は48000 Hz / 4chで開くが、
**4chすべてがサンプル単位で完全に一致する**（`np.array_equal`で確認、2回の録音とも）。
ビームフォーミング前の生アレイではなく、処理済みモノラル1本を4chに複製している。

→ **ダウンミックスは1ch取るだけでよい。**平均を取っても同じ値になるだけで無駄。
→ ストレージも4倍食っている。文字起こしへ渡す前に1chへ落とす。

## 2系統のフォーマットは揃わない前提で組む（実測値）
| | 自分（マイク） | 相手（ループバック） |
|---|---|---|
| レート | 48000 Hz | **96000 Hz** |
| チャンネル | 4ch（実体はモノラル） | 2ch |

リスク5が実機で出た。**倍のレート差**なので、統合前のリサンプルは必須。
`proto/dual-capture`はデバイスの設定のまま書くので、WAVの2本もこの通りに落ちる。
なお同じ機械でも、イヤホンを抜くと既定が48000 Hzのデバイスへ移った。
**レートは機械ではなくエンドポイントで決まる。実行のたびに読む。**

## `buffer underrun or overrun`が毎回出る（未解決・要観察）
業務ノートでの3回の実行すべてで、両系統に出た（自分1〜2回 / 相手1〜2回、12〜30秒あたり）。
開発デスクトップでも12秒で1回出ていたので、機種固有ではない。

**サンプル欠損が起きているのか、60分で何回になるのかは未確認。**
マイク側は音量が極小だった回にストリームが死んでおり、無害と決めつけられない。

**負荷で増える兆候がある。**回り込みテスト（再生ループ・ビープ・画面更新が同時に走る条件）では
30秒で**自分7回 / 相手9回**まで増えた。それまでは12〜30秒で1〜2回。
60分テストでは、録音以外に何を走らせているかも併せて記録する。

**開発デスクトップでは発生時刻が常にストリーム開始直後**（改修後の8秒の再計測で0.03秒＝
最初のコールバック）。業務ノートは12〜30秒あたりで出ていたので、**開始時の1回と、
録音中に出るものは別種**の可能性がある。`*-meta.json`に発生時刻を残すようにしたので、
60分テストでは分布で見分けられる。

## マイクの「録れた長さ」が経過時間を一貫して超える（未解決・要観察）
| 実行 | 実経過 | 録れた長さ | 差 |
|---|---|---|---|
| 12秒 | 12.01秒 | 12.12秒 | +0.11秒 |
| 30秒 | 30.00秒 | 30.22秒 | +0.22秒 |
| 15秒 | 15.00秒 | 15.07秒 | +0.07秒 |

**符号が常に正。**起動時のプリバッファぶんの固定オフセットなら無害だが、
**レート誤差（クロックドリフト）なら60分で20秒以上ずれる。**リスク4がここに顔を出している。
短時間の計測では固定オフセットとレート誤差を区別できない。**60分の連続録音で切り分ける。**

## Windows側の環境構築でハマった点（業務ノート、2026-09-03）
業務ノートにはRust・Visual Studio・Windows SDKのいずれも無く、`run.cmd`が動かなかった。
管理者権限を避けてGNUツールチェーンで通したが、素直には入らない。

- **rustupのGNUツールチェーンだけでは`windows`クレートがビルドできない。**
  `windows-link`等がraw-dylibを使い、rustcが外部の`dlltool.exe`を呼ぶ。
  `dlltool.exe`はツールチェーンの`lib/rustlib/<target>/bin/self-contained/`に同梱されているが
  **PATHに載っていない**。載せると今度は`dlltool: CreateProcess`で落ちる ——
  dlltoolが内部で呼ぶ**`as.exe`（GNUアセンブラ）が同梱されていない**ため。
  → MinGW本体（winget `BrechtSanders.WinLibs.POSIX.MSVCRT`）を入れてPATHに通すと通った。
- **容量**: `.rustup` 881 MB（`rust-docs`を外した後）/ `.cargo` 330 MB / WinLibs 923 MB。
  `profile default`だと`rust-docs`が693 MB乗る。**`rustup component remove rust-docs`で削れる。**
- **PowerShell 5.1はBOM無しUTF-8の`.ps1`をCP932として読む。**日本語がそのまま化け、
  文字列中のバイトが引用符に化けてパースエラーになる。**`.ps1`はBOM付きUTF-8で書く。**
- **cmd.exeはUNCパス（`\\wsl.localhost\...`）をcwdにできない。**
  `--manifest-path`で明示するか、Windows側に置いたバッチから叩く。
- `.exe`の標準出力をWSL側で読むときは、コンソール経由だとCP932で化ける。
  **ファイルへリダイレクトしてWSLから読む**のが確実。

## `cargo run`のラッパーを書くときは`--manifest-path`を`--`より前に置く
`cargo <args> --manifest-path <path>`の形でラッパーを書くと、呼び出し側が
`run --release -- --list`を渡した時点で`--manifest-path`が`--`の**後ろ**へ回り、
cargoではなくプログラムの引数になって`could not find Cargo.toml`で落ちる。
`run.cmd`は`cargo run --release --manifest-path "%~dp0Cargo.toml" -- %*`と正しく書けており、
`run.cmd --list`はそのまま動くことを実機で確認した。

## 要約は voice-input と逆の結論になる（サブスクの Claude が選択肢に入る）
voice-input では「**Claude Code のサブスク（`claude -p`）は使わない**」と決めた。プロセス起動と
巨大なシステムプロンプトで TTFT が持たないため。**この判断はレイテンシ要件から出ている。**

このPJは停止後に数十秒かけてよいので、**同じ前提が効かない**。プロセス起動の数秒は誤差になり、
代わりに **API 従量課金がゼロ**、かつ**上位モデルを気軽に使える**という利点だけが残る。
voice-input の結論をそのまま持ち込むと、ここで判断を誤る。

- 引き換えに、**開発で使う利用枠を会議の要約が食う**。長い会議が続く日は開発側が細る。
- 会議の中身を外に出す点は API 経由でも同じ。既に「LLM 要約で外に出す分は明示的に選ぶ」方針なので
  新しい論点ではない。所属組織のポリシーに照らす判断はユーザーが行う。

### 入力長は制約にならない（見積もり）
日本語の話し言葉は毎分およそ300字。60分の会議で実質45分ぶん発話があるとして 1.3万字、
多めに見て2万字。トークンにしても数万で、200K のコンテキストには十分収まる。
**「LLM の入力上限が効く」という当初のリスク認識は過大**だった。
残る本丸は**文字起こしの所要時間とメモリ**の方。

ただし**上限に収まることと、質が保たれることは別**。長い入力は中盤が薄くなるので、
章ごとに段階要約してから束ねる設計は、上限対策としてではなく**品質対策として**依然有効。
