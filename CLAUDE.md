# CLAUDE.md — meeting-scribe

このファイルは Claude Code へのプロジェクト指示書。**会話開始時に必ず読む。**
共通の進行管理・Git・記録ルールは グローバル `~/.claude/CLAUDE.md` に従う。
ここには**このPJ固有のことだけ**を書く。

## このプロジェクト固有の前提
- Slack / Teams の打ち合わせを録り、**要約テキストを作る**ツールを自作する。
  フロー: 開始ホットキー → マイク（自分）と PC 再生音（相手）を**別トラックで**録音 →
  停止ホットキー → 文字起こし → 話者ラベル付きで時系列に統合 → LLM で要約 → テキストファイル出力。
- **2系統を別トラックで録ることが本体価値**。混ぜて1本にすると誰の発言か落ちる。
  相手の声は WASAPI ループバック（PC の再生音）から取る。
- **実行環境は Windows 側**。WSL2 では動かさない（マイク・ループバック・グローバルホットキーが
  いずれも WSL では成立しない）。編集は WSL から行ってよいが、**動作確認は必ず Windows 側**で行う。
- **voice-input とは要求が正反対**。あちらは体感レイテンシがすべて、こちらは**正確さと要約の質**。
  録音停止後に数十秒かかってよい。速さのために精度を落とす判断はしない。
- **最大のリスク**：長時間音声（60分超）の扱い。文字起こし時間・メモリ・LLM の入力上限が
  一気に効いてくる。短い会議で動いても長い会議で壊れる、を前提に設計する。
- 実装言語・STT エンジンは未確定。voice-input（Rust + whisper-rs）の資産を流用できるかを
  最初に評価する。決めたら ADR として PROGRESS.md に記録する。

## 扱うデータの機微性（事故防止）
- **録音・文字起こし・要約は会議の中身そのもの。リポジトリに入れない**（`.gitignore` 済み）。
  出力先はリポジトリ外の作業ディレクトリにする。
- **音声をどこへ出すかは設計上の第一の判断**。クラウド STT に投げると会議内容が外部へ出る。
  業務の会議で使うなら**ローカル STT を既定**とし、外部送信は明示的に選んだ時だけにする。
- 録音は相手のいる場では所属組織のポリシーに従う。この判断はユーザーが行う（実装では強制しない）。

## API キーの扱い（voice-input と同じ・厳守）
- **`ANTHROPIC_API_KEY` を環境変数に設定しない。** グローバルに置くと WSL 側の Claude Code が
  拾い、サブスクではなく従量課金で動く事故になる。`~/.zshrc` / `~/.bashrc` に絶対書かない。
- キーは設定ファイル（**リポジトリ外**）か Windows Credential Manager に置く。**コミット厳禁。**
- voice-input 用に用意した専用 Anthropic アカウントを流用し、Console 側で spend limit を設定する。

## REQUIREMENTS.md の性質
- **機能仕様・スコープ**（ホットキー仕様、録音の系統、要約の出力形式、対応アプリ、品質目標）。
- 技術選定の未決事項もここの「未決事項」で管理し、決まったら ADR として PROGRESS.md に移す。

## Git 運用（グローバル §5 の差分）
- リモート: `https://github.com/jeeee-org/meeting-scribe`（**private**）
- デフォルトブランチ: `main`。低リスクな個人リポとして **§5.1 の例外を適用し、
  worktree を使わず main で直接作業する**。
- コミット規約: 日本語要約1行（§5.2 どおり。subject 50字目安 + body に「何を・なぜ・どう・影響範囲」）
- push 方針: グローバル §5 の既定どおり、1作業ごとに commit + 自動 push

## その他固有ルール
- ユーザーは日本語でやり取りする。ドキュメントも日本語で書く。
- **同一PCで voice-input が常駐している前提**。ホットキー・音声デバイスの取り合いに注意する。
