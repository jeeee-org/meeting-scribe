# 進捗

> 何が終わって、何が進行中で、次に何をやるか。**このファイルは常にスリムに保つ。**
> 範囲は REQUIREMENTS.md、学び/罠は NOTES.md、詳細ログは docs/checkpoints/。

最終更新: 2026-09-03

## 現在のフェーズ
**録れることは実証できたが、無音時の挙動は機種で割れる**（開発デスクトップ＝ゼロ埋め /
業務ノート＝再生中しかパケットが来ない）。統合を時刻ベースにする方針が固まった。
次は60分の連続録音で、クロックドリフトと underrun の累積を切り分ける。

## 次にやること (Top 3)
1. **`proto/dual-capture` を逐次書き出しへ改修** — 実測フォーマットのまま60分回すとメモリ
   約5.5GB で走らない。**60分テストの前提条件**。あわせてマイクは録る時点で1chへ落とす
2. **業務ノートで60分の連続録音** — マイクの「録れた長さ」が経過時間を超える件が
   固定オフセットかクロックドリフトかを切り分ける。underrun の累積回数と実害も同時に取る
3. **60分の文字起こし所要時間を実測** — 業務ノート（Intel iGPU）で現実的か。**ここが本丸**。
   持たないなら「録音は業務ノート → 文字起こしは開発デスクトップ」の2段構えにする

## 完了
- [x] 2026-09-03 初期構成を立ち上げ → [checkpoint](docs/checkpoints/2026-09-03.md)
- [x] 2026-09-03 ループバック録音を実証（開発デスクトップ）→ [checkpoint](docs/checkpoints/2026-09-03.md)
- [x] 2026-09-03 業務ノートで実測。無音時の挙動が機種で割れることを確認し、回り込み・
      エンドポイント・フォーマット差の前提を確定 → [checkpoint](docs/checkpoints/2026-09-03.md)

## 進行中
(なし)

## ADR（意思決定の記録）
- **ADR-001 録音は Rust + cpal 0.18 で作り、voice-input の資産を流用する**（2026-09-03）
  cpal は出力デバイスを入力として開くと WASAPI ループバックになる。プロトタイプで実証した。
- **ADR-002 アプリとして配布しない**（2026-09-03）
  対象PCがこのリポジトリを pull できるため `cargo run` で回す。配布まわりを v1 から外す。
- **ADR-003 耳を塞ぐ有線の機材を運用前提にする**（2026-09-03 / 09-03 実測で範囲を確定）
  開放型スピーカーだと相手の声がマイクに回り込み2系統の分離が壊れる。実測では
  **内蔵マイク＋有線イヤホンでも42.5dB の分離**が取れ、専用ヘッドセットは必須ではない。
- **ADR-004 2トラックの統合はサンプル数ではなく時刻で行う**（2026-09-03）
  業務ノート（Realtek）ではループバックが無音時にパケットを返さず、30秒の録音で5.59秒分しか
  届かなかった（ズレ +24.41秒）。開発デスクトップ（NVIDIA HDMI）ではゼロ埋めされたが、
  それはドライバ固有の挙動だった。サンプル数を時刻に換算する実装は、相手が黙った分だけ
  タイムラインが詰まって壊れる。コールバック到着時の実時刻を保持し、無音は時刻で埋める。
- **ADR-005 使用する音声エンドポイントは設定で固定する**（2026-09-03）
  ヘッドホンジャックのエンドポイントは抜き差しで一覧から消え、既定が別のデバイス
  （耳に届かないディスプレイ音声）へ移ることを実機で確認した。既定任せにすると
  無音のWAVができる。名前指定で固定し、録音開始時に採用したエンドポイント名をログへ残す。
