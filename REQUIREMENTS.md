# 要件 / スコープ

> 何を作るか・仕様/スコープ・未決事項。スコープが変わったら即更新する。

最終更新: 2026-09-04

## 目的・スコープ

Slack / Teams などで行う打ち合わせを、**開始と停止のホットキー2つだけ**で記録し、
終了後に**要約テキストファイル**を自動生成する。議事メモを取ることに気を取られず会話に集中したい、が狙い。

### やること（v1）
- グローバルホットキーで録音開始 / 停止（開始と停止は別キー、もしくは同一キーのトグル）
- **2系統の同時録音**
  - 自分 = マイク入力
  - 相手 = PC の再生音（WASAPI ループバック。Slack / Teams / ブラウザ会議の音を含む）
  - **別トラックで保持する**（混ぜない。話者の区別がこの2系統から得られる）
- 停止後に文字起こし（2トラックそれぞれ）
- タイムスタンプで時系列に統合し、`自分:` / `相手:` のラベル付きトランスクリプトを作る
- LLM で要約し、テキストファイルとして出力
- 出力はリポジトリ外の作業ディレクトリ（日付 + 時刻のファイル名）

### 要約の中身（初期案・未確定）
- 一言サマリ
- 決まったこと
- 自分のToDo / 相手のToDo（期限があれば添える）
- 未解決の論点・次回への持ち越し
- （生トランスクリプトも別ファイルで残す。要約が外した時に遡れるようにする）

### やらないこと（v1 では対象外）
- リアルタイム字幕・会議中の逐次表示
- 相手が複数人いる時の話者分離（v1 では相手はまとめて「相手」）
- Slack / Teams の API 連携（画面共有の内容・チャット欄の取り込み）
- 要約の自動投稿・自動メール送信
- Windows 以外のプラットフォーム対応
- **アプリとしての配布**（インストーラ・トレイ常駐・自動更新）

## 運用の前提（機材・実行形態）
- **耳を塞ぐ有線の機材で会議に出る**。開放型スピーカー（**ネックスピーカーを含む**）だと
  相手の声がマイクに回り込み、2系統の分離＝このPJの本体価値が壊れる。理由は NOTES.md「音の経路」。
  実測では**内蔵マイク＋有線イヤホンでも 42.5dB の分離**が取れており、専用ヘッドセットは必須ではない。
  ネックスピーカーで出た会議は「要約の質が落ちる会議」として割り切る。
- **アプリ化しない。** 対象PCはこのリポジトリを pull できるので `cargo run` で足りる。
  ホットキー常駐が要るかも、実運用で不便を感じてから決める。
- **使う音声エンドポイントは設定で固定する**（ADR-005）。ヘッドホンジャックのエンドポイントは
  抜き差しで一覧から消え、既定が耳に届かないデバイスへ移る。既定任せは無音の WAV を生む。

### 実機の構成（業務ノート ThinkPad L13 Gen 4 / `21LCS4M800`）
| | 機材 / エンドポイント | レート | 備考 |
|---|---|---|---|
| 自分 | Logi USB Headset H340（マイク） | 48000 Hz | **録音音量 -4.0 dB（47.3%）**。+2.3 dB ではクリップした |
| 相手 | Logi USB Headset H340（出力・ループバック） | **44100 Hz** | 2ch。ダウンミックスしない |

指定は `run.cmd <秒数> --mic=Logi --loopback=Logi`。回り込みは実測 45.1 dB で問題なし。
相手側の音量は、ブラウザ再生ならシステム 50% / プレーヤー 10% で RMS -31 dBFS・
ピーク -4.1 dBFS（70分間 ±1 dB で安定、クリップなし）。文字起こしの入力として良好。
録音音量はデバイスごとに別設定なので、**挿し直したら録音前に確認する**（余裕は 2.2 dB しかない）。
レートはエンドポイントで決まる（同じ機械でヘッドホンジャックは 96000 Hz、内蔵は 48000 Hz）。
**統合前のリサンプルは実行時に読んだ値で組み、定数で書かない。**

## 非機能要件

| 項目 | 目標 |
|---|---|
| 対応する会議の長さ | **60分**を確実に通す（120分でも壊れない） |
| 停止 → 要約ファイルができるまで | 会議30分あたり **3分以内**（＝実時間比 0.1）。**暫定値のまま未達**。業務ノートの CPU では実時間比 3.353（33倍足りない）。**見直しか実行環境の変更が要る** |
| 文字起こしの精度 | 固有名詞・術語は語彙辞書で補正（voice-input の `vocabulary` と同じ考え方） |
| 会議内容の外部送信 | 既定でゼロ（ローカル STT）。LLM 要約で外に出す分は明示的に選ぶ |
| API コスト | 月 $5 以内（spend limit で強制）。要約をサブスクの Claude で回すなら追加コストは無い（未決） |

## 対象 / バックログ
1. 2系統録音の最小プロトタイプ（マイク + ループバックを別 WAV に落とすだけ）
2. 文字起こしと時系列統合（話者ラベル付きトランスクリプト）
3. LLM 要約とファイル出力
4. 長時間録音の安定化（ドリフト・underrun の実害確認。ADR-002 により配布・常駐化はやらない）
5. 長時間会議への対応（分割・逐次処理）
6. 語彙辞書・要約フォーマットの調整（実運用で育てる）

## 未決事項
- [ ] **文字起こしをどこで走らせるか**（**いま最大の未決**）
      → 業務ノートの CPU では実時間比 3.353 で、60分の会議に3.4時間かかる（NOTES.md 第5回）。
      公式に Vulkan のビルド済みバイナリは無く、この機械の GPU 化は自前ビルドで数GB要る。
      開発デスクトップ（RTX 5080）なら cuBLAS のビルド済みがあり実時間比 0.02〜0.05 が堅い。
      → **2段構えにするなら、会議の音声を業務ノートの外へ出してよいかの判断が要る**
      （所属組織のポリシー。CLAUDE.md のとおりユーザーが判断する）
- [ ] **要約が「いつ」必要か** — 会議直後か、その日のうちでよいか。
      ここで実時間比の要件が決まる。ノート単体で完結させられるかもこれ次第
- [ ] **無音カット（VAD）を入れるか** — 2トラックぶんの文字起こし時間をおよそ半分にでき、
      無音区間での幻聴も減らせる。ただし速度問題そのものの解にはならない
- [ ] ~~業務ノートで長時間の文字起こしが現実的か~~（ThinkPad L13 / Intel iGPU）→ **測定済み。現実的でない**
- [ ] **文字起こしの単位**：停止後に一括か、録音中に区切って逐次流すか
      → 逐次なら停止後の待ち時間が消えるが、実装は重くなる
- [ ] **要約の実行手段**：Anthropic API（`claude-haiku-4-5` 等）か、**サブスクの Claude（`claude -p`）**か
      → 停止後に数十秒かけてよいので、voice-input と違いサブスク経由が成立する（NOTES.md 参照）。
      追加コストが無い代わりに、開発で使う利用枠を会議の要約が食う
- [ ] **時系列統合の方法**：2トラックのタイムスタンプをどう突き合わせるか（発話単位の粒度）
- [ ] **ホットキーが要るか**：アプリ化しない方針なので、まずは `cargo run` で開始・Ctrl+C で停止でも足りる。
      不便なら voice-input と衝突しないキーを割り当てる
- [ ] **出力先のパスとファイル名規約**
- [ ] 自分が無言の区間・相手が無言の区間の扱い（無音カットをどこでやるか）
