# 要件 / スコープ

> 何を作るか・仕様/スコープ・未決事項。スコープが変わったら即更新する。

最終更新: 2026-09-03

## 目的・スコープ

Slack / Teams などで行う打ち合わせを、**開始と停止のホットキー2つだけ**で記録し、
終了後に**要約テキストファイル**を自動生成する。議事メモを取ることに気を取られず会話に集中したい、が狙い。

### やること（v1）
- グローバルホットキーで録音開始 / 停止（開始と停止は別キー、もしくは同一キーのトグル）
- **2系統の同時録音**
  - 自分 = マイク入力
  - 相手 = PC の再生音（WASAPI ループバック。Slack / Teams / ブラウザ会議の音を含む）
  - **別トラックで保持する**（混ぜない。話者の区別がこの2系統から得られる）
- 停止後に文字起こし（2トラックそれぞれ）
- タイムスタンプで時系列に統合し、`自分:` / `相手:` のラベル付きトランスクリプトを作る
- LLM で要約し、テキストファイルとして出力
- 出力はリポジトリ外の作業ディレクトリ（日付 + 時刻のファイル名）

### 要約の中身（初期案・未確定）
- 一言サマリ
- 決まったこと
- 自分のToDo / 相手のToDo（期限があれば添える）
- 未解決の論点・次回への持ち越し
- （生トランスクリプトも別ファイルで残す。要約が外した時に遡れるようにする）

### やらないこと（v1 では対象外）
- リアルタイム字幕・会議中の逐次表示
- 相手が複数人いる時の話者分離（v1 では相手はまとめて「相手」）
- Slack / Teams の API 連携（画面共有の内容・チャット欄の取り込み）
- 要約の自動投稿・自動メール送信
- Windows 以外のプラットフォーム対応

## 非機能要件

| 項目 | 目標 |
|---|---|
| 対応する会議の長さ | **60分**を確実に通す（120分でも壊れない） |
| 停止 → 要約ファイルができるまで | 会議30分あたり **3分以内**（暫定。実測して見直す） |
| 文字起こしの精度 | 固有名詞・術語は語彙辞書で補正（voice-input の `vocabulary` と同じ考え方） |
| 会議内容の外部送信 | 既定でゼロ（ローカル STT）。LLM 要約で外に出す分は明示的に選ぶ |
| API コスト | 月 $5 以内（spend limit で強制） |

## 対象 / バックログ
1. 2系統録音の最小プロトタイプ（マイク + ループバックを別 WAV に落とすだけ）
2. 文字起こしと時系列統合（話者ラベル付きトランスクリプト）
3. LLM 要約とファイル出力
4. ホットキー・常駐化（voice-input と同じ流儀。トレイ + install.cmd）
5. 長時間会議への対応（分割・逐次処理）
6. 語彙辞書・要約フォーマットの調整（実運用で育てる）

## 未決事項
- [ ] **実装言語**：voice-input（Rust + whisper-rs + cpal）の資産を流用するか、別で作るか
      → 流用できるなら録音・STT・設定・トレイがほぼ揃う。最初に評価する
- [ ] **STT エンジン**：ローカル（whisper large-v3-turbo）か、長時間向けに別の選択があるか
      → voice-input の実績ではローカル GPU。ただしあちらは数秒の音声、こちらは数十分。**同じ結論とは限らない**
- [ ] **文字起こしの単位**：停止後に一括か、録音中に区切って逐次流すか
      → 逐次なら停止後の待ち時間が消えるが、実装は重くなる
- [ ] **要約に使うモデル**：`claude-haiku-4-5` か、質を取って上位モデルか
      → 長い入力を要約するので、速さより質が効く可能性が高い
- [ ] **時系列統合の方法**：2トラックのタイムスタンプをどう突き合わせるか（発話単位の粒度）
- [ ] **ループバック録音の実現手段**：WASAPI loopback を直接叩くか、既存クレート/ライブラリか
      → **相手の音が取れるかがこのPJの成否そのもの。最初に実機で確かめる**
- [ ] **ホットキーの割り当て**：voice-input と衝突しないキーにする（同一PCで常駐する前提）
- [ ] **対象PC**：業務ノートPC（ThinkPad L13, Intel iGPU）か開発デスクトップ（RTX 5080）か
      → 会議をするのは業務PC。だが iGPU では長時間の文字起こしが厳しい可能性がある
- [ ] **出力先のパスとファイル名規約**
- [ ] 自分が無言の区間・相手が無言の区間の扱い（無音カットをどこでやるか）
